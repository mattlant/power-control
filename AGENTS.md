# Agent Instructions

ticket-system: `linear`

This repository does not publish artifacts to git remotes. Read `.artifacts/AGENTS.md` when creating, modifying, or reading any artifacts under `.artifacts` directory.
